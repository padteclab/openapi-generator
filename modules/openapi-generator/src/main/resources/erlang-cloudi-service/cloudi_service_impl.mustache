-module({{packageName}}_impl).

-export([cloudi_service_handle_request/11]).

-include("{{packageName}}_error.hrl").
-include("{{packageName}}_cloudi_utils.hrl").

-record(state,
    {
    }).


cloudi_service_handle_request(_RequestType, _Name, _Pattern, _RequestInfo,
                              _Request, _Timeout, _Priority, _TransId, _Pid,
                              State, _Dispatcher) ->
  ?CLOUDI_REPLY({?NOT_IMPLEMENTED, [?ENOTIMPLEMENTED]}, State).
