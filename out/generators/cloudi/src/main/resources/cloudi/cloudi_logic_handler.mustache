-module({{packageName}}_cloudi_logic_handler).

-export([cloudi_service_handle_request/11]).

-record(state,
    {
    }).

cloudi_service_handle_request(_RequestType, _Name, _Pattern,
                              _RequestInfo, _Request,
                              _Timeout, _Priority,
                              _TransId, _Pid,
                              State, _Dispatcher) ->
    Code = 501,
    Response = [{<<"error">>, [
                  {<<"code">>, Code},
                  {<<"message">>, <<"Got not implemented request to process">>}]}],
    {Code, Response}.
